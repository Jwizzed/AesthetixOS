{
  "name": "aesthetix-os-monorepo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"frontend-*\" run dev",
    "build": "pnpm -r --filter \"frontend-*\" run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "postcss": "^8.4.49",
    "autoprefixer": "^10.4.20"
  }
}
